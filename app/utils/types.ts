export type Project = {
  name: string;
  description: string;
  github: string;
  liveUrl?: string;
  imageUrl?: string;
};

export type Certificate = {
  name: string;
  organization: string;
  date: string;
  skills: string;
  credential: string;
};
